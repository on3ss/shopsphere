spring:
  application:
    name: product-service

  devtools:
    restart:
      enabled: true
      poll-interval: 1s
      quiet-period: 400ms
    livereload:
      enabled: true

  config:
    import: optional:configserver:http://localhost:8888
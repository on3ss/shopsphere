apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-order
  namespace: shopsphere
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-order
  template:
    metadata:
      labels:
        app: postgres-order
    spec:
      containers:
        - name: postgres
          image: postgres:18
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: order_db
            - name: POSTGRES_USER
              value: order
            - name: POSTGRES_PASSWORD
              value: order
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "order", "-d", "order_db"]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "order", "-d", "order_db"]
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        # emptyDir keeps this simple for local/dev use — data is lost when the
        # pod is recreated. Swap for a PVC if you need it to survive restarts.
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-order
  namespace: shopsphere
spec:
  selector:
    app: postgres-order
  ports:
    - port: 5432
      targetPort: 5432

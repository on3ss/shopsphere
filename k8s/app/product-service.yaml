apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: shopsphere
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
        - name: product-service
          image: shopsphere/product-service
          imagePullPolicy: Never
          ports:
            - containerPort: 8081
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: kubernetes
            - name: SPRING_CONFIG_IMPORT
              value: optional:configserver:http://config-server:8888
            # spring-boot.run.fork=false (set in the Dockerfile CMD) means
            # Maven's own JVM runs the app directly — one process per pod,
            # not two — so these options apply to that single JVM.
            # TieredStopAtLevel=1 skips the C2 JIT tier: slower steady-state
            # throughput, faster boot, which is the trade you want for a
            # process that restarts on every hot reload.
            - name: JAVA_TOOL_OPTIONS
              value: "-Xmx512m -XX:MaxMetaspaceSize=192m -XX:TieredStopAtLevel=1 -XX:+UseSerialGC"
          resources:
            requests:
              cpu: 150m
              memory: 384Mi
            limits:
              cpu: 500m
              memory: 768Mi
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: shopsphere
spec:
  selector:
    app: product-service
  ports:
    - port: 8081
      targetPort: 8081
